<% @page_title = @user.name %>

<div class="container">
  <div class="row">
    <div class="col-md-4">
      <%= render 'partials/user_profile' %>
    </div>
    <div class="user-contents col-md-8">
      <ul class="user-posts">
        <%= render @microposts %>
      </ul>
      <%= paginate @microposts %>
    </div>
  </div>
</div>