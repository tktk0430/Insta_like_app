<footer>
  <div class="container">
    <% if login? %>
      <% unless params[:controller]=="microposts"&&params[:action]=="new" %>
        <%= link_to '新規投稿', new_micropost_path, class:"btn btn-primary btn-block"%>
      <% end %>
    <% else %>
      <h4><%= link_to '見て頂いた方へ（このサイトについて）', about_path %></h4>
    <% end %>
  </div>
</footer>